pipeline {
    agent {
        label "worker"
    }

    tools {
        maven 'mvn'
    }

    stages {

        stage('checkout') {
            steps {
                echo "Clonning is successful"
            }
        }

        stage('Build') {
            steps {
                sh '''
                  mvn clean package
                '''
            }
        }

        stage('Test') {
            steps {
                echo "Testing is successful"
            }
        }

        stage('Deploy') {
            steps {
                echo "Deployment is successful"
            }
        }
    }
}